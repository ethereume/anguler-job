import { ShowDisableDirective } from './show-disable.directive';

describe('ShowDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDisableDirective();
    expect(directive).toBeTruthy();
  });
});
